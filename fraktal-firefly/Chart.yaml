apiVersion: v2
name: fraktal-firefly
description: Fraktal FireFly deployment with Fabric support using official Hyperledger FireFly Helm Charts
type: application
version: 0.1.0
appVersion: "1.4.0"

maintainers:
  - name: Fraktal Team

dependencies:
  - name: firefly
    repository: "oci://ghcr.io/hyperledger/helm"
    version: "0.9.1"
    condition: firefly.enabled
