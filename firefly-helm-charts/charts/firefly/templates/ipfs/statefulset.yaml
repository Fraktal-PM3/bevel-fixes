# Copyright © 2024 Kaleido, Inc.
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://swww.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

{{- if .Values.ipfs.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "firefly.fullname" . }}-ipfs
  labels:
    {{- include "firefly.ipfsLabels" . | nindent 4 }}
spec:
  serviceName: {{ include "firefly.fullname" . }}-ipfs
  replicas: 1
  selector:
    matchLabels:
      {{- include "firefly.ipfsSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.ipfs.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "firefly.ipfsSelectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.ipfs.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.ipfs.podSecurityContext | nindent 8 }}
      initContainers:
      - name: init-ipfs
        image: "{{ .Values.ipfs.image.repository }}:{{ .Values.ipfs.image.tag }}"
        imagePullPolicy: {{ .Values.ipfs.image.pullPolicy }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          if [ ! -f /data/ipfs/config ]; then
            ipfs init --profile={{ .Values.ipfs.profile }}
            ipfs config Addresses.API /ip4/0.0.0.0/tcp/5001
            ipfs config Addresses.Gateway /ip4/0.0.0.0/tcp/8080
{{- if .Values.ipfs.swarmKey }}
cat > /data/ipfs/swarm.key <<'EOF'
{{ .Values.ipfs.swarmKey | trimSuffix "\n" }}
EOF
            ipfs bootstrap rm --all
{{- end }}
          fi
        volumeMounts:
        - name: ipfs-data
          mountPath: /data/ipfs
      containers:
      - name: ipfs
        securityContext:
          {{- toYaml .Values.ipfs.securityContext | nindent 10 }}
        image: "{{ .Values.ipfs.image.repository }}:{{ .Values.ipfs.image.tag }}"
        imagePullPolicy: {{ .Values.ipfs.image.pullPolicy }}
        command: ["ipfs", "daemon", "--migrate"]
        ports:
        - name: api
          containerPort: 5001
          protocol: TCP
        - name: gateway
          containerPort: 8080
          protocol: TCP
        - name: swarm
          containerPort: 4001
          protocol: TCP
        {{- with .Values.ipfs.extraEnv }}
        env:
        {{- toYaml . | nindent 8 }}
        {{- end }}
        livenessProbe:
          exec:
            command:
            - ipfs
            - --api=/ip4/127.0.0.1/tcp/5001
            - dag
            - stat
            - /ipfs/QmUNLLsPACCz1vLxQVkXqqLX5R1X345qqfHbsf67hvA3Nn
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - ipfs
            - --api=/ip4/127.0.0.1/tcp/5001
            - dag
            - stat
            - /ipfs/QmUNLLsPACCz1vLxQVkXqqLX5R1X345qqfHbsf67hvA3Nn
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          exec:
            command:
            - ipfs
            - --api=/ip4/127.0.0.1/tcp/5001
            - dag
            - stat
            - /ipfs/QmUNLLsPACCz1vLxQVkXqqLX5R1X345qqfHbsf67hvA3Nn
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        volumeMounts:
        - name: ipfs-data
          mountPath: /data/ipfs
        resources:
          {{- toYaml .Values.ipfs.resources | nindent 10 }}
      {{- with .Values.ipfs.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ipfs.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ipfs.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  volumeClaimTemplates:
  - metadata:
      name: ipfs-data
      {{- with .Values.ipfs.persistentVolume.annotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      accessModes:
        {{- toYaml .Values.ipfs.persistentVolume.accessModes | nindent 8 }}
      {{- if .Values.ipfs.persistentVolume.storageClass }}
      storageClassName: {{ .Values.ipfs.persistentVolume.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.ipfs.persistentVolume.size }}
{{- end }}
